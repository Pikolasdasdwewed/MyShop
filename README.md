📘 MyShop — Магазин Мониторов (Node.js + Express + MySQL)

Учебный проект интернет-магазина мониторов c авторизацией, ролями пользователей, CRUD-управлением товарами и REST API с документацией Swagger.

🚀 Возможности

Авторизация через сессии

Роли пользователей:

Admin — управление товарами

User — просмотр каталога

CRUD для мониторов (добавление, редактирование, удаление)

Автоматическое создание 9 демо-товаров при первом запуске

REST API для внешней интеграции

Swagger документация

Использование MySQL + Sequelize

📦 Установка
1. Клонировать проект
git clone https://github.com/USERNAME/REPOSITORY.git
cd REPOSITORY

2. Установить зависимости
npm install

3. Создать файл .env

Используйте шаблон:

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASS=
DB_NAME=MyShop

SESSION_SECRET=your_secret
PORT=3000

ADMIN_EMAIL=admin@myshop.local
ADMIN_PASSWORD=123

USER_EMAIL=user@myshop.local
USER_PASSWORD=123

🗄 Настройка MySQL

Создать БД:

CREATE DATABASE MyShop;


Таблицы создаются автоматически при запуске.

Сброс таблиц (без удаления базы):

node reset_database.js

▶️ Запуск проекта
npm start


После запуска:

Главная: http://localhost:3000

Swagger: http://localhost:3000/swagger.json

🔑 Доступ

Автоматически создаются, если таблица users пустая:

Администратор:

Email: из .env — ADMIN_EMAIL

Пароль: ADMIN_PASSWORD

Обычный пользователь:

Email: USER_EMAIL

Пароль: USER_PASSWORD

📁 Структура проекта
config/           — настройки MySQL
controllers/      — API и веб-контроллеры
middleware/       — авторизация и роли
models/           — Sequelize модели
public/           — CSS и изображения
routes/           — API и web маршруты
views/            — шаблоны EJS
swagger.json      — документация API
reset_database.js — сброс таблиц
app.js            — точка входа

📄 REST API (кратко)
Авторизация

POST /login — вход

POST /logout — выход

Товары

GET /api/products — список

POST /api/products — создать (admin)

GET /api/products/:id — один товар

PUT /api/products/:id — обновить (admin)

DELETE /api/products/:id — удалить (admin)

Подробности — в swagger.json.